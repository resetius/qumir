(block
  (use "Робот")
  (fun caller () -> <future void>
    (block
      (await (call helper))
      (cleanup-exit (return))))
  (fun helper () -> <future void>
    (block
      (await (call вверх))
      (cleanup-exit (return))))
  (fun pure ()
    (block
      (cleanup-exit (return)))))