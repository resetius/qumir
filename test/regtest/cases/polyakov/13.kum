алг Массив
нач
    цел i, N=10
    целтаб A[1:N]
    нц для i от 1 до N
        A[i] := 180 + i*i
    кц

    цел count=0, sum=0
    нц для i от 1 до N
        если 180<A[i] и A[i]<190 то
            count := count + 1
            sum := sum + A[i]
        все
    кц
    вывод count, ' ', sum, нс
кон