алг ЫШЧО
нач
   лит word = '...'
   TumbaWords('ЫШЧО', word, 0)
кон

алг TumbaWords(лит A, w0, цел N)
нач
   если N = длин(w0) то
      вывод w0, нс
      выход
   все
   цел i
   лит w
   w := w0[1:длин(w0)]
   нц для i от 1 до длин(A)
      w[N+1]:=A[i]
      TumbaWords(A, w, N+1)
   кц
кон