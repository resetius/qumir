использовать Комплексные числа

алг
нач
    компл a, b
    лог same, different

    a := 2.0 + 3.0*i
    b := 2.0 + 4.0*i

    same := a = a
    different := a <> b

    вывод "and=", same и different, нс
    вывод "or=", same или (a = b), нс
    вывод "not=", не (a = b), нс
кон
