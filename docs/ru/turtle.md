# Исполнитель Черепаха

**Черепаха** — графический исполнитель для создания рисунков с помощью команд перемещения. Черепаха «ходит» по полю и оставляет след — линию.

## Основы

Черепаха находится на плоскости и имеет:
- **Позицию** (x, y) — координаты на плоскости
- **Направление** — угол в градусах (0° — вправо, 90° — вверх)
- **Перо** — опущено (рисует) или поднято (не рисует)

## Команды

### Перемещение

| Команда | Описание |
|---------|----------|
| `вперед(n)` | Двигаться вперёд на n шагов |
| `назад(n)` | Двигаться назад на n шагов |
| `вправо(угол)` | Повернуть вправо на угол градусов |
| `влево(угол)` | Повернуть влево на угол градусов |

### Управление пером

| Команда | Описание |
|---------|----------|
| `опустить_перо` | Опустить перо (черепаха рисует) |
| `поднять_перо` | Поднять перо (черепаха не рисует) |

### Дополнительные

| Команда | Описание |
|---------|----------|
| `в_точку(x, y)` | Переместиться в точку (x, y) |
| `установить_угол(угол)` | Установить направление |
| `установить_цвет(r, g, b)` | Установить цвет линии (RGB 0-255) |
| `установить_толщину(n)` | Установить толщину линии |

## Примеры

### Квадрат

```
алг
нач
    нц для i от 1 до 4
        вперед(100)
        вправо(90)
    кц
кон
```

### Треугольник

```
алг
нач
    нц для i от 1 до 3
        вперед(100)
        вправо(120)
    кц
кон
```

### Звезда

```
алг
нач
    нц для i от 1 до 5
        вперед(100)
        вправо(144)
    кц
кон
```

### Окружность (приближённо)

```
алг
нач
    нц для i от 1 до 360
        вперед(2)
        вправо(1)
    кц
кон
```

### Спираль

```
алг
нач
    вещ длина
    длина := 5
    нц для i от 1 до 50
        вперед(длина)
        вправо(90)
        длина := длина + 5
    кц
кон
```

### Цветной рисунок

```
алг
нач
    | Красный квадрат
    установить_цвет(255, 0, 0)
    нц для i от 1 до 4
        вперед(50)
        вправо(90)
    кц
    
    | Перемещаемся без рисования
    поднять_перо
    вперед(100)
    опустить_перо
    
    | Синий квадрат
    установить_цвет(0, 0, 255)
    нц для i от 1 до 4
        вперед(50)
        вправо(90)
    кц
кон
```

## Фракталы

### Снежинка Коха

```
алг
нач
    нц для i от 1 до 3
        кох(200, 4)
        вправо(120)
    кц
кон

алг кох(вещ длина, цел глубина)
нач
    если глубина = 0
    то
        вперед(длина)
    иначе
        кох(длина / 3, глубина - 1)
        влево(60)
        кох(длина / 3, глубина - 1)
        вправо(120)
        кох(длина / 3, глубина - 1)
        влево(60)
        кох(длина / 3, глубина - 1)
    все
кон
```

### Дерево

```
алг
нач
    влево(90)
    дерево(100, 5)
кон

алг дерево(вещ длина, цел глубина)
нач
    если глубина > 0
    то
        вперед(длина)
        влево(30)
        дерево(длина * 0.7, глубина - 1)
        вправо(60)
        дерево(длина * 0.7, глубина - 1)
        влево(30)
        назад(длина)
    все
кон
```

### Треугольник Серпинского

```
алг
нач
    серпинский(200, 5)
кон

алг серпинский(вещ размер, цел глубина)
нач
    если глубина = 0
    то
        нц для i от 1 до 3
            вперед(размер)
            вправо(120)
        кц
    иначе
        серпинский(размер / 2, глубина - 1)
        вперед(размер / 2)
        серпинский(размер / 2, глубина - 1)
        назад(размер / 2)
        влево(60)
        вперед(размер / 2)
        вправо(60)
        серпинский(размер / 2, глубина - 1)
        влево(60)
        назад(размер / 2)
        вправо(60)
    все
кон
```

## Использование в онлайн-песочнице

1. Откройте [qumir.dev](https://qumir.dev)
2. Напишите программу с командами черепахи
3. Нажмите **Run**
4. Переключите вывод на **Черепаха** (появится автоматически)

## Система координат

- Начало координат (0, 0) — в центре поля
- Ось X направлена вправо
- Ось Y направлена вверх
- Углы отсчитываются от направления вправо против часовой стрелки

## См. также

- [Исполнитель Робот](robot.md)
- [Примеры программ](examples.md)
